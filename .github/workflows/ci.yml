name: CI

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main, develop]

jobs:
  backend-tests:
    runs-on: ubuntu-latest
    services:
      postgres:
        image: postgres:15
        env:
          POSTGRES_HOST_AUTH_METHOD: trust
          POSTGRES_DB: indel_test
        options: >-
          --health-cmd "pg_isready -U postgres"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
        ports:
          - 5432:5432

    steps:
      - uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.25.x'

      - name: Run backend tests
        env:
          DB_HOST: localhost
          DB_PORT: 5432
          DB_USER: postgres
          DB_PASSWORD: ""
          DB_NAME: indel_test
        run: |
          cd backend
          go test -v ./...

      - name: Run linter
        uses: golangci/golangci-lint-action@v6
        with:
          working-directory: backend
          version: v1.64.8
          install-mode: goinstall

  ml-tests:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r ml/requirements.txt
          pip install pytest pytest-cov

      - name: Run ML tests
        run: |
          cd ml
          pytest .

  frontend-tests:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: '18'

      - name: Install dependencies (insurer)
        run: |
          cd insurer-dashboard
          npm install

      - name: Build insurer dashboard
        run: |
          cd insurer-dashboard
          npm run build

      - name: Install dependencies (platform)
        run: |
          cd platform-dashboard
          npm install

      - name: Build platform dashboard
        run: |
          cd platform-dashboard
          npm run build

  docker-build:
    runs-on: ubuntu-latest
    needs: [backend-tests, ml-tests]

    steps:
      - uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build backend image
        uses: docker/build-push-action@v6
        with:
          context: ./backend
          push: false
          tags: indel-backend:latest

      - name: Build premium ML image
        uses: docker/build-push-action@v6
        with:
          context: ./ml
          file: ./ml/premium/Dockerfile
          push: false
          tags: indel-premium-ml:latest

      - name: Build fraud ML image
        uses: docker/build-push-action@v6
        with:
          context: ./ml
          file: ./ml/fraud/Dockerfile
          push: false
          tags: indel-fraud-ml:latest

      - name: Build forecast ML image
        uses: docker/build-push-action@v6
        with:
          context: ./ml
          file: ./ml/forecast/Dockerfile
          push: false
          tags: indel-forecast-ml:latest
